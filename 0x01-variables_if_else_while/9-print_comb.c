#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always equals 0
 *
 */
int main(void)
{
	int a = '0';

	while (a <= '9')
	{
		putchar(a);
		if (n != '9')
		{
			putchar(',');
			putchar(' ');
		}
		a++;
	}
	putchar('\n');

	return (0);
}
