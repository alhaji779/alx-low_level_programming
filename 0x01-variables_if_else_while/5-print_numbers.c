#include <stdio.h>
/**
 * main - Entry point
 *
 * Program to display numbers
 *
 * return: always 0
 */
int main(void)
{
	int a = 0;

	for (; a < 10; a++;)
	{
		printf(a);
	}

	printf('\n');

	return (0);
}
