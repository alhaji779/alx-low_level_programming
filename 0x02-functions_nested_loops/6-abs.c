#include "main.h"
/**
 * _abs - converts to absolute value
 *
 * @c: - requires an integer arg
 *
 * Return: absolute value
 *
 */
int _abs(int c)
{

	if (c > 0)
	{
		return (c);
	}
	else
	{
		return (c * -1);
	}
}
