#include "main.h"
/**
 * _abs - converts to absolute value
 *
 * @int: - requires an integer arg
 *
 * Return: absolute value
 *
 */
int _abs(int)
{
	int c;

	if (c > 0)
	{
		return (c);
	}
	else
	{
		return (c * -1);
	}
}
